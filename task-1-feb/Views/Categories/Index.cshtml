@model IEnumerable<task_1_feb.Category>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @*@Html.DisplayNameFor(model => model.CategoryName)*@
            <span>Category</span>
        </th>
        <th>
            @*@Html.DisplayNameFor(model => model.Description)*@
            <span>Description</span>
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CategoryName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CategoryID }, new { @class = "btn btn-success btn-responsive" }) |
            @Html.ActionLink("Details", "Details", new { id=item.CategoryID }, new { @class = "btn btn-warning btn-responsive" }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CategoryID }, new { @class = "btn btn-danger btn-responsive" })
        </td>
    </tr>
}

</table>
